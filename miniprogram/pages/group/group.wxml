<view class="container">
  <!-- 美化后的标题 -->
  <view class="title-wrapper">
    <text class="title">选择分组</text>
  </view>

  <!-- 分组列表 -->
  <view wx:for="{{groups}}" wx:key="id" class="group-item" data-id="{{item.id}}" bindtap="onSelectGroup">
    <text>{{item.name}}</text>
  </view>
</view>
